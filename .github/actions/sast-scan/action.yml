name: SAST Scan
description: Static application security testing (demo shared action).
runs:
  using: composite
  steps:
    - shell: bash
      run: |
        echo "Running SAST scan — shared action v2 (intentionally failing for demo)"
        exit 1
